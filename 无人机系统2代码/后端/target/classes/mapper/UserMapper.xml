<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.md.basePlatform.mapper.UserMapper">

    <!-- 数据库字段到 Java 属性的映射 -->
    <resultMap id="BaseResultMap" type="com.md.basePlatform.domain.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <!-- 根据 ID 查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, password, status, create_time, update_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <!-- 插入用户，自动回填自增 ID -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{status}, #{createTime}, #{updateTime})
    </insert>

</mapper>